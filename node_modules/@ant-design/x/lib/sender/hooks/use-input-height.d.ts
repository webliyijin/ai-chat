import type { SenderProps } from '../interface';
declare const useInputHeight: (styles: React.CSSProperties, autoSize: SenderProps['autoSize'], editableRef: React.RefObject<HTMLDivElement | null>) => React.CSSProperties;
export default useInputHeight;
